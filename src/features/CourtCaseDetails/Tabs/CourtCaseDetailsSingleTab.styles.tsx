import Image from "next/image"
import styled from "styled-components"

const CheckmarkIcon = styled(Image)`
  display: inline-block;
  vertical-align: bottom;
  margin-top: -2px;
`

export { CheckmarkIcon }
