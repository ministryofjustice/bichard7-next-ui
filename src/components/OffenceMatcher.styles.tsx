import styled from "styled-components"
import { SaveLinkButton } from "./LinkButton"

const StyledSaveButton = styled(SaveLinkButton)`
  margin-top: 0;
  margin-left: 5px;
  display: inline;
`

export { StyledSaveButton }
